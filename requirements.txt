fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pyyaml>=6.0
python-multipart>=0.0.9
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6.23
pypdf>=4.0
numpy>=1.24

# --- Optional: local speech-to-text for the 🎤 mic button (See/Listen/Talk) ---
# Install ONE of these on the GB10 to enable transcription. Without either, the
# mic returns a clear 501 and everything else works unchanged.
#   faster-whisper>=1.0        # recommended (GPU; decodes browser audio directly)
#   openai-whisper>=20231117   # fallback (also needs the system `ffmpeg` binary)
